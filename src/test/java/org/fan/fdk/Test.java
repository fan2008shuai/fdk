package org.fan.fdk;


public class Test {

    private void sort(int[] array) {
        int p = -1;
        for (int i = 0; i < array.length; i++) {
            if (array[i] % 2 == 0) {
                continue;
            } else {
//                swap(array[++p], array[i]);
            }
        }
    }

}
